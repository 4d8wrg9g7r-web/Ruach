{
  "buildCommand": "pnpm --filter @ruach/database exec prisma migrate deploy && pnpm --filter @ruach/database exec prisma generate && next build",
  "crons": [
    {
      "path": "/api/cron/sync",
      "schedule": "0 * * * *"
    },
    {
      "path": "/api/cron/usage-warnings",
      "schedule": "0 13 * * *"
    }
  ]
}
